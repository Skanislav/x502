{
  "name": "@x502/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny standalone project used as the x502 demo subject. Has a planted bug, missing tests, and a stale doc — each one is bountied on the vault.",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "derive-commitment": "tsx scripts/derive-commitment.ts"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "@x502/shared": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
